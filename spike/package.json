{
  "name": "ganttero-spike-voz",
  "version": "0.1.0",
  "private": true,
  "description": "Fase 0 — spike aislado: audio → STT (Whisper) → Gemma 4 → JSON de tarea validado con Zod",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
